# Monte-Carlo-Simulation_Ayush
Implement a Monte Carlo (MC) simulation of a Lennard-Jones fluid in the canonical (NVT) ensemble using the Metropolis algorithm in Scilab, and to study the potential energy per particle ⟨U⟩/N as a function of MC steps for N = 100 particles at reduced temperature T* = 1.0.
